config | arch: | info: Goa testbed

+ common_routes
  + service ROM | label_last: ld.lib.so | + parent
  + service CPU                         | + parent
  + service PD                          | + parent
  + service LOG                         | + parent
  + service Timer                       | + parent

+ start fonts_fs | priority: -2 | pkg: fonts_fs
  + route
    + service ROM | label: config | + parent | label: config -> managed/fonts

+ start recall_fs | priority: -2 | pkg: recall_fs
  + route
    + service File_system | + child default_fs_rw

+ start black_hole | priority: -2 | pkg: black_hole

+ start sntp_dummy_rtc | pkg: ssumpf/pkg/sntp_dummy_rtc/2025-10-27
  + route
    + service Nic | + child nic_router

+ start gpu | caps: 1500 | pkg: a64_lima_gpu-pinephone
  + route
    + service Platform | + parent | label: gpu

+ start goa_testbed | pkg: ssumpf/pkg/goa_testbed/2025-10-10 | priority: -2
  + route
    + service TRACE                            | + parent
    + service VM                               | + parent
    + service Gpu                              | + parent
    + service Gui                              | + parent
    + service Rtc                              | + child sntp_dummy_rtc
    + service Play                             | + child black_hole
    + service Record                           | + child black_hole
    + service Usb                              | + child black_hole
    + service Event                            | + child black_hole
    + service Capture                          | + child black_hole
    + service File_system                      | + child recall_fs
    + service Nic | label_prefix: tcp_terminal | + child nic_router | label: telnet
    + service Nic | label_prefix: lighttpd     | + child nic_router | label: http
    + service Nic                              | + child nic_router | . for test scenario
    + service Report | label_last: clipboard   | + parent
    + service Report | label_last: shape       | + parent
    + service ROM | label_last: clipboard      | + parent
    + service ROM | label: platform_info       | + parent | label: platform_info
    + service ROM | label: capslock            | + parent | label: capslock
    + service ROM | label: mesa_gpu.lib.so     | + parent | label: mesa_gpu-lima.lib.so
-
